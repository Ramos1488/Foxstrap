<Window x:Class="Foxstrap.Windows.SetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Установка Foxstrap"
        Width="500" Height="380"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        WindowStyle="None"
        AllowsTransparency="True"
        Background="Transparent">
    <Border Background="#1E1E2E" CornerRadius="12" BorderBrush="#313244" BorderThickness="1">
        <Grid Margin="40">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Лого -->
            <StackPanel Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,0,0,24">
                <Image Source="pack://application:,,,/Assets/foxstrap.png" Width="48" Height="48" RenderOptions.BitmapScalingMode="HighQuality"/>
                <StackPanel VerticalAlignment="Center" Margin="14,0,0,0">
                    <TextBlock Text="Foxstrap" FontSize="28" FontWeight="Bold" Foreground="#CDD6F4"/>
                    <TextBlock Text="Лаунчер для Roblox" FontSize="13" Foreground="#A6ADC8"/>
                </StackPanel>
            </StackPanel>

            <!-- Статус -->
            <TextBlock x:Name="StatusText" Grid.Row="1"
                       Text="Добро пожаловать в Foxstrap!"
                       FontSize="14" Foreground="#CDD6F4"
                       HorizontalAlignment="Center" Margin="0,0,0,16"
                       TextWrapping="Wrap" TextAlignment="Center"/>

            <!-- Прогресс-бар -->
            <Border Grid.Row="2" Background="#313244" CornerRadius="4" Height="6"
                    VerticalAlignment="Center" Margin="0,0,0,24">
                <Border x:Name="ProgressFill" Background="#CBA6F7" CornerRadius="4"
                        HorizontalAlignment="Left" Width="0"/>
            </Border>

            <!-- Кнопка установить -->
            <Button x:Name="InstallButton" Grid.Row="3"
                    Content="Установить и настроить Roblox"
                    Click="Install_Click"
                    HorizontalAlignment="Stretch"
                    Padding="0,14" Margin="0,0,0,10"
                    Background="#CBA6F7" Foreground="#1E1E2E"
                    BorderThickness="0" FontSize="14" FontWeight="SemiBold"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Кнопка запустить Roblox -->
            <Button x:Name="LaunchButton" Grid.Row="4"
                    Content="Запустить Roblox"
                    Click="Launch_Click"
                    HorizontalAlignment="Stretch"
                    Padding="0,14" Margin="0,0,0,10"
                    Background="#313244" Foreground="#CDD6F4"
                    BorderThickness="0" FontSize="14"
                    Cursor="Hand" IsEnabled="False">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>

            <!-- Кнопка настроек -->
            <Button x:Name="SettingsButton" Grid.Row="5"
                    Content="Открыть настройки лаунчера"
                    Click="Settings_Click"
                    HorizontalAlignment="Stretch"
                    Padding="0,14"
                    Background="#313244" Foreground="#CDD6F4"
                    BorderThickness="0" FontSize="14"
                    Cursor="Hand">
                <Button.Template>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" CornerRadius="8" Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Button.Template>
            </Button>
        </Grid>
    </Border>
</Window>
